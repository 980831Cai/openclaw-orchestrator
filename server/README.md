# OpenClaw Orchestrator

Multi-Agent Visual Orchestration Plugin for OpenClaw.

## Quick Start

```bash
# Install
pip install -e .

# Run
openclaw-orchestrator serve
```

## Development

```bash
# Install with dev dependencies
pip install -e ".[dev]"

# Run with auto-reload
openclaw-orchestrator serve --reload
```
